Required:
NODE_ENV=production
DATABASE_URL=${{Postgres.DATABASE_URL}}
SESSION_SECRET=<strong random secret>
OPENAI_API_KEY=<server-side key>
OPENAI_MODEL=gpt-5.6-luna

Optional:
TEACHER_INVITE_CODE=<private invite code>

Never commit .env or real secrets.
